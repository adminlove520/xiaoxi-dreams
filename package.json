{
  "name": "superdreams",
  "version": "4.1.0",
  "description": "SuperDreams (超梦) — AI Agent 认知记忆系统，通过做梦实现记忆整合与进化",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix agent",
    "build": "npm run build --prefix agent",
    "vercel-build": "cd agent && npm install && npm run build",
    "start": "npm run start --prefix agent",
    "dev:center": "npm run dev --prefix center",
    "build:center": "npm run build --prefix center",
    "dream": "node scripts/dream.js"
  },
  "keywords": [
    "ai",
    "agent",
    "memory",
    "dreaming",
    "cognitive"
  ],
  "author": "adminlove520",
  "license": "MIT"
}
